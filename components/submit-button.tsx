/**
 * Form submit button with pending state.
 *
 * Sketch: useFormStatus, disabled + spinner while pending. Every form in the app
 * uses this, so double-submit is handled once.
 *
 * Test: tests/components/submit-button.test.tsx
 */
