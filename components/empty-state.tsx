/**
 * Empty state.
 *
 * Stories: SP-020, SP-023, SP-064, SP-073
 *
 * Sketch: icon, headline, body, one call to action. Used everywhere a list can
 * legitimately be empty. Four stories in the backlog exist because empty is a
 * state we keep forgetting to design — so it gets one component.
 */
