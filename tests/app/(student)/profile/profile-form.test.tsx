/**
 * RTL tests for app/(student)/profile/profile-form.tsx.
 *
 * Stories: SP-021, SP-022
 *
 * Cases
 *  - email, role and status render as read-only, with no form control
 *  - interests are populated from the ACTIVE catalog only
 *  - client-side validation shows the same message the server would
 *  - submit is disabled while pending
 */
