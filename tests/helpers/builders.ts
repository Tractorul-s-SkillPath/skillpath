/**
 * Test data builders.
 *
 * Sketch: aQuestion(), anAssessment(), aResponse(), aProfile(), aPlanItem() —
 * each returning a valid object with sensible defaults and an overrides
 * argument.
 *
 * Builders, not fixtures-by-copy-paste: a test should state only the field it
 * cares about, so the reader can see what the case is actually about.
 */
