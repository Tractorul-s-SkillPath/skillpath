/**
 * Shared constant fixtures.
 *
 * Sketch: the canonical 10-question answer key, a fully-correct response set,
 * a half-correct one, an all-unanswered one, and the per-category score maps
 * that the level / weak-area / plan tests all share.
 *
 * Sharing these is what makes scoring -> level -> weak areas -> plan testable
 * as a chain, with one agreed definition of "a mediocre result".
 */
