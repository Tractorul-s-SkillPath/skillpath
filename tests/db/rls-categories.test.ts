/**
 * RLS: skill_categories.
 *
 * Stories: SP-004, SP-032
 *
 * Cases
 *  - a student sees active categories only
 *  - an inactive category is invisible to a student and visible to an admin
 *  - a student cannot insert, update or delete a category
 *  - an admin can do all three (subject to on delete restrict)
 */
