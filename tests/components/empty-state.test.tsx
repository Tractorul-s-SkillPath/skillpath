/**
 * RTL tests for components/empty-state.tsx.
 *
 * Stories: SP-020, SP-023, SP-064, SP-073
 *
 * Cases
 *  - renders headline, body and the call-to-action link
 *  - it is a heading + link, not a decorative div — the empty state is often a
 *    new user's first screen
 */
