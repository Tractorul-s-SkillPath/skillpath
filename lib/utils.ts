/**
 * Tiny shared helpers.
 *
 * Sketch: cn() (clsx + tailwind-merge, required by shadcn/ui), and date/number
 * formatting used in more than one place.
 *
 * This file is NOT a dumping ground. Anything with a business rule in it belongs
 * in lib/domain, where the coverage gate can see it.
 */
