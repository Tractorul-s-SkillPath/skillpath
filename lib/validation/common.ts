/**
 * Shared primitives.
 *
 * Sketch: id (positive int coerced from a form string), uuid, paginationSchema
 * (page, pageSize with a MAX — no endpoint returns an unbounded set, SP-086),
 * trimmedString(min, max), and the enum schemas mirroring the six SQL enums.
 *
 * Test: tests/lib/validation/common.test.ts
 */
