/**
 * Category schema.
 *
 * Stories: SP-031, SP-032
 *
 * Sketch: name trimmed 2-60 (the same bounds as the SQL check constraint —
 * that duplication is deliberate and tested), description, status enum.
 *
 * Test: tests/lib/validation/category.schema.test.ts
 */
