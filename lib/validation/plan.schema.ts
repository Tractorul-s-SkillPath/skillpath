/**
 * Plan schemas.
 *
 * Story: SP-063
 *
 * Sketch: updateStatusSchema = { recommendationId, progressStatus: enum }.
 * topic_title and priority are absent by design (SP-063 AC3).
 *
 * Test: tests/lib/validation/plan.schema.test.ts
 */
