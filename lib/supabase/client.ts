/**
 * Browser client — anon key + the user's session.
 *
 * Story: SP-002
 *
 * Sketch: createBrowserClient. Only for interactive work that genuinely must not
 * round-trip. In this app that is close to nothing: answers are saved through a
 * Server Action, not from here.
 */
