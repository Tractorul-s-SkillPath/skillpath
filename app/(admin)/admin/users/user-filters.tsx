/**
 * User search + filters.
 *
 * Stories: SP-083, SP-085
 *
 * Sketch: same URL-state pattern as the question filters. Do not invent a second
 * one — extract the shared bit into components/search-input.tsx.
 */
