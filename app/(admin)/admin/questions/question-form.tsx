/**
 * Create / edit a question.
 *
 * Stories: SP-034, SP-035
 *
 * Sketch
 *  - text, category, difficulty (all required) + <AnswerEditor />
 *  - new questions are created status='inactive' by default — nothing goes live
 *    without a deliberate activation
 *  - editing an existing question warns that past results keep their stored
 *    is_correct snapshots (D4)
 */
