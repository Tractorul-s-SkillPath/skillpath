/**
 * Generation request form.
 *
 * Story: SP-092
 *
 * Sketch: category, difficulty, count (capped — see lib/ai/guardrails.ts).
 * Pending state is long; show it honestly rather than pretending it is instant.
 */
