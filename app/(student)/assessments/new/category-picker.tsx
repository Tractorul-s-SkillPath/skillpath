/**
 * Category + level picker — client component.
 *
 * Stories: SP-040, SP-048
 *
 * Sketch
 *  - single select for the MVP; multi-select is SP-048 (N assessments, one
 *    session_id) and is the first thing cut if Week 5 is tight
 *  - categories with too few questions are shown disabled with the reason,
 *    not hidden — a silent absence looks like a bug
 */
