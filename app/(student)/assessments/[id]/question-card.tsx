/**
 * One question + its options.
 *
 * Stories: SP-043, SP-038
 *
 * Sketch
 *  - radio group, keyboard accessible, labelled
 *  - its props type comes from the answer_options view: answer_id, answer_text,
 *    position. There is no is_correct field to accidentally render.
 */
